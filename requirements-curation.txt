-r requirements.txt

# CPU curation and tokenizer environment. GPU training hosts install
# requirements-gpu.txt, whose Hugging Face stack intentionally uses a newer
# major version that cannot coexist with the pinned Datatrove 0.9.0 recipe.
transformers==4.57.6
datasets==5.0.0
tokenizers==0.22.2
huggingface-hub==0.36.2
fsspec[http]==2026.4.0

# ── Data curation ─────────────────────────────────────────────────────────────
trafilatura>=1.9.0
ftfy>=6.3.1,<7.0.0
fasttext-wheel>=0.9.2
warcio>=1.7.4
datatrove[processing]==0.9.0
spacy>=3.7.0

# ── Validation ────────────────────────────────────────────────────────────────
sentencepiece>=0.2.0,<0.3.0
# kenlm — install from source (not on PyPI):
#   make install-kenlm
#   or: pip install https://github.com/kpu/kenlm/archive/master.zip


# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0
pandas>=2.2.0
