apiVersion: v1
kind: Service
metadata:
  name: slm-service
  namespace: inference
  labels:
    app: slm
spec:
  selector:
    app: slm
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP