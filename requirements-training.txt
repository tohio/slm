-r requirements.txt

# GPU training stack. Keep the repo's validated CUDA build, independent of GPU count.
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
transformers==5.14.1
accelerate==1.14.0
trl==1.9.0
datasets==5.0.0
tokenizers==0.22.2
huggingface-hub==1.24.0
fsspec[http]==2026.4.0
liger-kernel==0.8.2

# Artifact restore, experiment tracking, and training utilities.
wandb>=0.17.0

# Evaluation runs in the training environment; no separate evaluation install.
evaluate>=0.4.0
lm-eval==0.4.9
