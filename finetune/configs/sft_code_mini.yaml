name: slm-mini-code
wandb_project: slm-mini
model:
  base_model_path: $RESULTS_DIR/runs/mini/sft_instruct/final
  max_seq_length: 1024
data:
  train_path: $DATA_DIR/runs/mini/sft_code/train.jsonl
  val_path: $DATA_DIR/runs/mini/sft_code/val.jsonl
  packing: false
  loss_type: chunked_nll
  min_retention_ratio: 0.90
  max_samples: 500
training:
  max_steps: 50
  warmup_ratio_recipe: 0.03
  micro_batch_size: 2
  gradient_accumulation_steps: 2
  precision: bf16
  gradient_clip_val: 1.0
  gradient_checkpointing: false
  eval_steps: 25
  save_steps: 25
  save_total_limit: 1
  log_steps: 5
  num_workers: 2
  seed: 42
  lr_scheduler: cosine
  report_to:
  - wandb
optimizer:
  lr: 5.0e-06
  weight_decay: 0.01
  beta1: 0.9
  beta2: 0.98
