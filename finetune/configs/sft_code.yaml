name: slm_sft_code

trainer:
  devices: 1
  num_nodes: 1
  accelerator: gpu
  precision: bf16
  logger: false
  enable_checkpointing: false
  max_epochs: 3
  log_every_n_steps: 10
  val_check_interval: 200
  limit_val_batches: 50
  gradient_clip_val: 1.0

exp_manager:
  explicit_log_dir: /results/slm_sft_code
  name: ${name}
  create_wandb_logger: false
  wandb_logger_kwargs:
    project: slm
    name: sft_code
  resume_if_exists: true
  resume_ignore_no_checkpoint: true
  create_checkpoint_callback: true
  checkpoint_callback_params:
    monitor: val_loss
    save_top_k: 3
    mode: min
    save_last: true
    filename: slm_sft_code--{val_loss:.2f}-{step}

model:
  # Sequential fine-tuning: load from chat SFT checkpoint, not pre-train.
  # This preserves conversational ability while adding code capability.
  restore_from_path: /results/slm_sft_chat/checkpoints/last.nemo

  tensor_model_parallel_size: 1
  pipeline_model_parallel_size: 1

  # Only compute loss on assistant responses — not the human prompt tokens.
  # Requires GPTSFTModel from nemo_aligner (not base MegatronGPTModel).
  answer_only_loss: true
  sample_weight: constant

  # ── Data ────────────────────────────────────────────────────────────────────
  data:
    chat: true
    num_workers: 4
    train_ds:
      file_path: /data/sft/code/train.jsonl
      max_seq_length: 2048
      micro_batch_size: 4
      global_batch_size: 32
      shuffle: true
      add_eos: true
    validation_ds:
      file_path: /data/sft/code/val.jsonl
      max_seq_length: 2048
      micro_batch_size: 4
      global_batch_size: 32
      shuffle: false
      add_eos: true

  # ── Optimizer ───────────────────────────────────────────────────────────────
  # Lower LR than chat SFT (5e-6 vs 1e-5) to reduce catastrophic forgetting
  # of chat capabilities while adapting to code tasks.
  optim:
    name: fused_adam
    lr: 5.0e-6
    weight_decay: 0.01
    betas: [0.9, 0.98]
    sched:
      name: CosineAnnealing
      warmup_steps: 50
      constant_steps: 0
      min_lr: 5.0e-7

  # ── Memory ──────────────────────────────────────────────────────────────────
  # Must match chat SFT checkpoint settings to load weights correctly
  activations_checkpoint_method: uniform
  activations_checkpoint_num_layers: 1
  activations_checkpoint_granularity: full