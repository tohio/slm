# Full development environment. GPU training hosts install the smaller
# requirements-gpu.txt; local curation/eval/notebook work uses this file.
-r requirements-training.txt

# ── Evaluation ────────────────────────────────────────────────────────────────
evaluate>=0.4.0

# ── Data curation ─────────────────────────────────────────────────────────────
trafilatura>=1.9.0
fasttext-wheel>=0.9.2
warcio>=1.7.4
datatrove[processing]>=0.2.0
orjson>=3.9.0
spacy>=3.7.0

# ── Validation ────────────────────────────────────────────────────────────────
# kenlm — install from source (not on PyPI):
#   make install-kenlm
#   or: pip install https://github.com/kpu/kenlm/archive/master.zip

lm-eval>=0.4.2

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0
pandas>=2.2.0
