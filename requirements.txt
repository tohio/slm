--extra-index-url https://download.pytorch.org/whl/cu121
# ── Core ──────────────────────────────────────────────────────────────────────
# torch 2.5.1 is the highest version available for CUDA 12.1.
# trl 0.29 is compatible with torch 2.5.1 — trl does not require FSDPModule
# from PyTorch 2.6 until later versions (that constraint was introduced, then
# relaxed, in the 0.27–0.29 range).
torch==2.5.1+cu121
torchvision==0.20.1+cu121

# ── HuggingFace — pinned to compatible v5 stack ───────────────────────────────
# transformers v5 is a breaking change from v4:
#   - GenerationMixin separated from PreTrainedModel
#   - _tied_weights_keys is a dict (not a list)
#   - cache_position added to prepare_inputs_for_generation
#   - DynamicCache moved to transformers.cache_utils
#   - PreTrainedModel.__init__ no longer sets `warnings_issued` — this affects
#     trl 0.29's DPOTrainer, which assigns to model.warnings_issued["estimate_tokens"].
#     train_dpo.py pre-seeds the attribute to compensate. trl PR #4960 (post-0.29,
#     pre-1.0) removes the access — when we upgrade past 0.29, remove the hack.
# All four libraries must be upgraded together — mixing versions breaks training.
transformers>=5.5.0,<5.6.0
accelerate>=1.6.0,<2.0.0
trl==0.29.0
datasets>=3.0.0,<4.0.0
tokenizers>=0.22.0,<0.24.0
huggingface-hub>=1.5.0,<2.0.0
evaluate>=0.4.0

# trl 0.29 optional dependencies pulled in at import time
weave
mergekit

# ── Data curation ─────────────────────────────────────────────────────────────
trafilatura>=1.9.0
fasttext-wheel>=0.9.2
warcio>=1.7.4
datatrove[processing]>=0.2.0
orjson>=3.9.0
spacy>=3.7.0

# ── Validation ────────────────────────────────────────────────────────────────
# kenlm — install from source (not on PyPI):
#   make install-kenlm
#   or: pip install https://github.com/kpu/kenlm/archive/master.zip

# ── AWS / S3 ──────────────────────────────────────────────────────────────────
boto3>=1.34.0
botocore>=1.34.0

# ── Experiment tracking ───────────────────────────────────────────────────────
wandb>=0.17.0

# ── Evaluation ────────────────────────────────────────────────────────────────
lm-eval>=0.4.2

# ── Utilities ─────────────────────────────────────────────────────────────────
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0.1
python-dotenv>=1.0.0
requests>=2.31.0

# ── Notebooks ─────────────────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0
pandas>=2.2.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.0.0